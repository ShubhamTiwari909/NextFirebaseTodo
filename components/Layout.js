function Layout({ children }) {
    return (
        <>
            {/* NAVBAR */}
           
            <main>
                {children}
            </main>

        </>

    )
}

export default Layout